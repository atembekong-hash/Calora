CALORA — STEP 40 RELEASE GATE PERMISSION REMEDIATION AND VERIFIED EXECUTION

OUTPUT REPORT:

40_CALORA_RELEASE_GATE_PERMISSION_REMEDIATION_REPORT.md


==================================================
OWNER AUTHORIZATION
==================================================

Step 39 concluded:

CONTROLLER ROOT CAUSE PARTIALLY IDENTIFIED — OWNER GITHUB EVIDENCE REQUIRED


The owner has now supplied the requested GitHub UI evidence.

GitHub Actions displays this exact validation failure for:

.github/workflows/release-validation.yml

ERROR:

Invalid workflow file: .github/workflows/release-validation.yml#L1

(Line: 12, Col: 3): Unexpected value 'administration'


This owner evidence CONFIRMS that:

permissions:
  administration: read

is invalid GitHub Actions workflow syntax in this workflow.

This explains why the Release validation workflow produced:

- a workflow run object;
- immediate failure;
- zero jobs;
- zero check runs;
- no runner allocation;
- no step logs.


Current canonical GitHub main before Step 40 is expected to be:

SHA:
11eb7d0038f2d60b8540cc2f027d2b50c2117b91

TREE:
d3c230005c043874b9e245b70cd4b23f70af8105


I authorize the MINIMUM remediation necessary to:

1. remove the proven invalid workflow permission declaration;
2. preserve the release-validation gate;
3. determine a supported authentication/authorization design for the
   required-check audit;
4. implement ONLY the minimum safe solution if it can be proven locally and
   from GitHub-supported behavior;
5. push the resulting canonical main normally;
6. allow the automatic Release validation workflow to execute;
7. verify the exact required check on the exact new canonical SHA.


This authorization DOES NOT include:

- weakening branch protection;
- removing the required release check;
- renaming the required release check;
- disabling the audit without replacement;
- application feature changes;
- production API changes;
- database changes;
- migrations;
- native application changes;
- Expo/EAS configuration changes;
- iOS build;
- Android build;
- TestFlight submission;
- App Store submission;
- API deployment;
- Replit production publish;
- force push;
- history rewriting.


==================================================
MISSION 1 — FREEZE CURRENT STATE
==================================================

Fetch origin read-only.

Record:

origin/main SHA
origin/main TREE

local main SHA
local main TREE

ahead/behind
working-tree status


Expected remote canonical identity:

SHA:
11eb7d0038f2d60b8540cc2f027d2b50c2117b91

TREE:
d3c230005c043874b9e245b70cd4b23f70af8105


Classify every local-only commit.

Step 39 had local documentation/evidence commits.

Do not confuse documentation with runtime changes.


If origin/main has advanced unexpectedly:

STOP.

Do not push.

Final verdict:

PERMISSION REMEDIATION BLOCKED — REMOTE MAIN CHANGED


==================================================
MISSION 2 — RECORD THE OWNER-CONFIRMED ROOT CAUSE
==================================================

Record the new owner GitHub UI evidence exactly:

Invalid workflow file: .github/workflows/release-validation.yml#L1

(Line: 12, Col: 3): Unexpected value 'administration'


Classify the previous uncertainty as resolved:

PROVEN:

permissions.administration is invalid in the GitHub Actions workflow
permissions block.


Do not continue treating this specific finding as hypothetical.


==================================================
MISSION 3 — RECONFIRM WORKFLOW AND BRANCH PROTECTION
==================================================

Before editing, reconfirm:

Workflow:
.github/workflows/release-validation.yml

Workflow name:
Release validation

Job ID:
release-validation

Job display name:
Run release validation suite

Main branch required context:
Run release validation suite


Confirm current workflow contains:

permissions:
  contents: read
  administration: read


Confirm push trigger remains:

push:
  branches:
    - main


Confirm:

pull_request
workflow_dispatch

remain present.


Do not edit branch protection.


==================================================
MISSION 4 — AUDIT THE REQUIRED-CHECK SCRIPT BEFORE FIXING AUTH
==================================================

Inspect:

scripts/audit-required-checks.mjs


Document exactly:

- which GitHub REST endpoint(s) it calls;
- which HTTP method(s);
- what data it reads;
- what conditions cause success;
- what conditions cause failure;
- whether it modifies anything;
- which environment variables it requires;
- whether it specifically requires GITHUB_TOKEN;
- whether another supported token source is already implemented;
- whether it can inspect repository source/configuration without calling the
  protected branch API;
- whether its purpose is runtime security, release integrity, or merely
  configuration consistency.


Do NOT modify the script yet.


==================================================
MISSION 5 — DETERMINE THE SUPPORTED AUTHORIZATION MODEL
==================================================

This is critical.

The previous invalid design attempted to obtain branch-protection read access
with:

permissions:
  administration: read


That is NOT a valid Actions workflow permission.

Determine what GitHub officially supports for the exact branch-protection
read operation used by:

scripts/audit-required-checks.mjs


Separate these questions:

A. What permissions can GitHub Actions GITHUB_TOKEN request?

B. What permission does the branch-protection REST endpoint document?

C. Can the standard Actions GITHUB_TOKEN access that endpoint in this
   repository despite not having an `administration` workflow key?

D. If not, what supported credential type is required?

Examples that may need evaluation:

- GitHub App installation token;
- fine-grained PAT;
- another supported read-only repository credential;
- alternative GitHub API endpoint accessible to GITHUB_TOKEN;
- redesigning the audit to validate source-controlled configuration rather
  than protected settings.


DO NOT invent a token.

DO NOT expose credentials.

DO NOT create a PAT.

DO NOT create a GitHub App.

DO NOT add a fake secret.

DO NOT weaken branch protection.


==================================================
MISSION 6 — PREFER ZERO-SECRET SOLUTION IF PROVABLY SAFE
==================================================

Prefer a solution that:

- requires no new long-lived credential;
- does not weaken branch protection;
- preserves the required check;
- remains deterministic;
- remains auditable;
- works on pull_request and push;
- cannot silently pass when the required check is misconfigured.


But do NOT force a zero-secret solution if GitHub cannot support it safely.

If the audit genuinely requires an external read credential and none already
exists:

STOP before inventing one.

Produce:

PERMISSION SYNTAX FIX IDENTIFIED — SUPPORTED AUDIT CREDENTIAL REQUIRED


In that case, specify exactly what owner action would be required next.


==================================================
MISSION 7 — MINIMUM PROVEN WORKFLOW FIX
==================================================

At minimum, remove the now-proven invalid line:

administration: read


The resulting workflow permissions must contain only GitHub-supported workflow
permission keys.


Preserve:

contents: read


Do NOT broaden permissions unnecessarily.

Do NOT use:

write-all


Do NOT grant write permissions merely to make the workflow pass.


==================================================
MISSION 8 — AUDIT SCRIPT DECISION
==================================================

After removing the invalid workflow permission, determine whether
audit-required-checks.mjs can safely run using the normal:

${{ github.token }}


If PROVEN YES:

preserve the audit step using the supported token and continue.


If PROVEN NO:

determine whether an already-existing authorized read credential is available
WITHOUT revealing its value.

If a suitable credential already exists:

document its secret NAME only and prove its intended scope/usage without
printing the secret.

Do not rotate or replace it.


If no suitable credential exists:

DO NOT bypass the audit.

DO NOT delete the audit.

DO NOT hard-code success.

DO NOT change branch protection.

STOP before push unless the workflow can remain semantically valid and secure.


==================================================
MISSION 9 — DO NOT HIDE THE PROBLEM
==================================================

The following are forbidden:

- removing audit-required-checks.mjs simply to get green CI;
- replacing the audit with `exit 0`;
- ignoring 401/403 responses;
- converting failures to warnings;
- `continue-on-error: true`;
- weakening required status checks;
- deleting main branch protection;
- changing the required check to an unrelated workflow;
- giving GITHUB_TOKEN broad write permissions;
- hard-coding expected GitHub settings without a trustworthy validation path.


The gate must remain meaningful.


==================================================
MISSION 10 — STATIC VALIDATION AFTER AUTHORIZED EDIT
==================================================

After the minimum edit, validate:

- YAML parsing;
- formatting;
- GitHub Actions workflow structure;
- all workflow permission keys against GitHub-supported Actions permissions;
- push main trigger;
- pull_request trigger;
- workflow_dispatch trigger;
- workflow name;
- job ID;
- exact job display name;
- runner;
- timeout;
- actions;
- audit command;
- test command.


Run:

pnpm --filter @workspace/scripts test

pnpm run typecheck

git diff --check


All must pass before push.


==================================================
MISSION 11 — DIFF SAFETY AUDIT
==================================================

Inspect the complete candidate diff.

Expected configuration change should be extremely small.

At minimum:

.github/workflows/release-validation.yml


If audit script modification is genuinely necessary and independently justified,
the only additional authorized candidate is:

scripts/audit-required-checks.mjs


But modify that script ONLY if necessary for a proven supported authorization
design.


No changes are authorized under:

artifacts/calora/
artifacts/api-server/
lib/db/
lib/api-spec/
lib/api-client-react/
lib/api-zod/
Expo config
EAS config
native app configuration
production API configuration
database schema
migrations


If unrelated changes appear:

STOP.

DO NOT PUSH.


==================================================
MISSION 12 — PRE-PUSH DECISION GATE
==================================================

Before committing/pushing, classify the remediation:

CASE A:

Invalid permission removed AND audit has a proven supported authentication path.

Proceed.


CASE B:

Invalid permission removed BUT audit requires a credential that does not
currently exist.

DO NOT PUSH an intentionally broken release workflow.

STOP with:

PERMISSION SYNTAX FIX IDENTIFIED — SUPPORTED AUDIT CREDENTIAL REQUIRED


CASE C:

Additional unexplained workflow/controller problem discovered.

STOP with:

PERMISSION REMEDIATION INCOMPLETE — ADDITIONAL CONTROLLER FAILURE


==================================================
MISSION 13 — COMMIT
==================================================

Only for CASE A:

Create one clearly identified remediation commit.

Suggested subject:

Fix release validation workflow permissions


Record:

commit SHA
tree SHA
changed paths


The commit must contain no unrelated runtime/application changes.


==================================================
MISSION 14 — PRE-PUSH REMOTE SAFETY CHECK
==================================================

Fetch origin immediately before push.

Verify:

origin/main has not advanced unexpectedly.

Verify:

candidate is based on current origin/main ancestry.

Verify:

behind = 0.

Verify:

working tree clean.


If remote changed:

STOP.

DO NOT PUSH.


==================================================
MISSION 15 — NORMAL PUSH
==================================================

Only if all gates pass:

git push origin main


No:

--force
--force-with-lease
rebase
reset
history rewriting


Do not manually dispatch Release validation.

The normal main push must automatically create the validation run.


==================================================
MISSION 16 — PROVE THE INVALID-WORKFLOW ERROR IS GONE
==================================================

Observe the exact automatically created Release validation run for the new
canonical SHA.


First prove:

GitHub no longer reports:

Unexpected value 'administration'


Then prove:

job count > 0

check-run count > 0


A run object by itself is NOT sufficient.


==================================================
MISSION 17 — VERIFY REQUIRED CHECK IDENTITY
==================================================

There must be a check run named EXACTLY:

Run release validation suite


Record:

workflow run ID
run number
event
branch
head SHA
job ID
job name
check-run ID
check-run name
status
conclusion
started time
completed time


Head SHA MUST equal the new canonical GitHub main SHA.


==================================================
MISSION 18 — VERIFY EVERY WORKFLOW STEP
==================================================

Verify execution of:

1. checkout
2. required branch-check audit
3. pnpm setup
4. Node setup
5. dependency installation
6. Run release validation suite


Record status/conclusion for each.


If the audit step now reaches runtime but fails with 401 or 403:

THIS IS USEFUL EVIDENCE.

Do NOT bypass it.

Do NOT immediately add a credential.

Stop with:

RELEASE WORKFLOW EXECUTES — AUDIT AUTHORIZATION REMAINS BLOCKED


Record the exact HTTP status and sanitized error.


==================================================
MISSION 19 — REQUIRED-CHECK AUDIT VERIFICATION
==================================================

If audit-required-checks.mjs executes successfully, prove it actually verified:

main

required context:

Run release validation suite


Do not accept a no-op audit.

Record the exact sanitized evidence/result.


==================================================
MISSION 20 — SUCCESS CRITERIA
==================================================

The release gate is successful ONLY if all are true:

1. GitHub workflow is syntactically/semantically accepted.
2. Automatic push run created.
3. Job count > 0.
4. Required check run exists.
5. Check name exactly:
   Run release validation suite
6. Check head SHA equals canonical main SHA.
7. Checkout executes.
8. Required-check audit executes successfully.
9. pnpm setup executes.
10. Node setup executes.
11. dependencies install successfully.
12. release validation tests execute.
13. required check concludes:
    success
14. local main == origin/main.
15. ahead = 0.
16. behind = 0.


Anything less is NOT a verified release gate.


==================================================
MISSION 21 — POST-PUSH CANONICAL VERIFICATION
==================================================

If successful:

fetch origin again.

Record:

local main SHA
origin/main SHA

local TREE
origin TREE

ahead
behind
working-tree status


This becomes the new canonical candidate for later TestFlight authorization.


==================================================
MISSION 22 — APPLICATION INVARIANTS
==================================================

Confirm no Calora application behavior changed.

Preserve:

- durable capture acceptance;
- Weekly Programs deterministic modal state machine;
- shared Planner Program pools/eligibility;
- bounded Coach lifecycle;
- Coach Fact Context restriction;
- diary imageAssetKey synchronization;
- capture compatibility/security;
- recipe nextOffset;
- recipe terminalReason;
- PKCE/auth;
- account isolation;
- deletion fences;
- sync ownership;
- API contracts;
- Expo Router;
- EAS configuration;
- Apple Health privacy strings;
- TestFlight configuration.


==================================================
MISSION 23 — DATABASE / DEPLOYMENT BOUNDARY
==================================================

Confirm:

database changes: none
schema changes: none
migrations: none
API deployment command: none
Replit production publish: none
EAS build: none
iOS build: none
Android build: none
TestFlight submission: none
App Store submission: none


If an external provider automatically reacts to a main push, record that
separately and do not claim it as a Step 40 deployment action.


==================================================
MISSION 24 — NO TESTFLIGHT YET
==================================================

EVEN IF THE RELEASE GATE PASSES:

STOP.

DO NOT BUILD IOS.

DO NOT TRIGGER EAS.

DO NOT SUBMIT TESTFLIGHT.


Step 40 ends at verified release-gate success.

Native release remains separately owner-authorized.


==================================================
OUTPUT REPORT
==================================================

Create:

40_CALORA_RELEASE_GATE_PERMISSION_REMEDIATION_REPORT.md


Report MUST contain:

1. Executive summary
2. Owner GitHub UI error evidence
3. Confirmed root cause
4. Starting canonical SHA/tree
5. Local/origin state
6. Local-only commit classification
7. Branch-protection verification
8. Original workflow permissions
9. audit-required-checks.mjs forensic map
10. GitHub GITHUB_TOKEN permission analysis
11. Branch-protection REST authorization analysis
12. Supported authentication alternatives considered
13. Chosen minimum design or reason for stopping
14. Exact workflow diff
15. Any audit-script diff
16. Workflow syntax validation
17. Scripts test result
18. Typecheck result
19. git diff --check
20. Changed-path safety audit
21. Remediation commit SHA/tree if created
22. Pre-push remote verification
23. Push result if authorized by gates
24. New canonical SHA/tree if pushed
25. Automatic workflow run ID
26. Job count
27. Check-run count
28. Required check identity
29. Per-step execution results
30. Required-check audit result
31. Any runtime 401/403 evidence
32. Final required-check conclusion
33. Post-push local/origin synchronization
34. Application invariants
35. Database/migration confirmation
36. Deployment/mobile-build confirmation
37. Remaining uncertainties
38. Exact recommendation for Step 41


FINAL VERDICT MUST BE EXACTLY ONE OF:

RELEASE GATE VERIFIED — READY FOR OWNER TESTFLIGHT AUTHORIZATION

PERMISSION SYNTAX FIX IDENTIFIED — SUPPORTED AUDIT CREDENTIAL REQUIRED

RELEASE WORKFLOW EXECUTES — AUDIT AUTHORIZATION REMAINS BLOCKED

PERMISSION REMEDIATION INCOMPLETE — ADDITIONAL CONTROLLER FAILURE

PERMISSION REMEDIATION BLOCKED — REMOTE MAIN CHANGED

PERMISSION REMEDIATION FAILED VALIDATION — NOT PUSHED


==================================================
STOP CONDITION
==================================================

STOP after the permission remediation investigation, safe remediation if
supported, GitHub verification if pushed, and numbered report.

DO NOT BUILD IOS.

DO NOT BUILD ANDROID.

DO NOT TRIGGER EAS.

DO NOT SUBMIT TESTFLIGHT.

DO NOT SUBMIT APP STORE.

DO NOT DEPLOY API.

DO NOT MODIFY DATABASE.

Wait for explicit owner authorization for Step 41.