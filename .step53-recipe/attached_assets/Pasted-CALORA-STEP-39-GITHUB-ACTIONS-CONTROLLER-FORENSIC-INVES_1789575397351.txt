CALORA — STEP 39 GITHUB ACTIONS CONTROLLER FORENSIC INVESTIGATION

OUTPUT REPORT:

39_CALORA_GITHUB_ACTIONS_CONTROLLER_FORENSIC_REPORT.md


==================================================
OWNER AUTHORIZATION
==================================================

Step 38 concluded:

RELEASE GATE REMEDIATION INCOMPLETE — CONTROLLER FAILURE PERSISTS


Current canonical GitHub main:

SHA:
11eb7d0038f2d60b8540cc2f027d2b50c2117b91

TREE:
d3c230005c043874b9e245b70cd4b23f70af8105


Step 38 successfully corrected the missing main push trigger.

GitHub then automatically created:

Workflow:
Release validation

Run ID:
35120305466

Run number:
2

Event:
push

Branch:
main

Head SHA:
11eb7d0038f2d60b8540cc2f027d2b50c2117b91

Conclusion:
failure


But the run produced:

0 jobs
0 check runs
0 billable runners
no runner logs
no step logs
no annotations


Therefore:

DO NOT make another speculative workflow change.

The purpose of Step 39 is to determine WHY GitHub Actions terminates these
workflows before job initialization.


==================================================
STRICT SAFETY BOUNDARY
==================================================

STEP 39 IS DIAGNOSTIC ONLY.

DO NOT:

- edit application source;
- edit workflow files;
- edit GitHub Actions settings;
- edit branch protection;
- edit rulesets;
- edit required checks;
- change repository permissions;
- change organization settings;
- change secrets;
- change variables;
- change environments;
- change runner configuration;
- register/remove runners;
- commit remediation;
- push;
- merge;
- rebase;
- reset;
- force push;
- manually rerun workflows;
- manually dispatch workflows;
- trigger EAS;
- build iOS;
- build Android;
- submit TestFlight;
- submit App Store;
- deploy API;
- publish Replit production;
- modify database;
- run migrations.

Investigation only.

The numbered report may be created locally as documentation evidence, but it
must NOT be pushed in Step 39.


==================================================
MISSION 1 — FREEZE CURRENT CANONICAL STATE
==================================================

Fetch origin read-only.

Record:

origin/main SHA
origin/main TREE

local main SHA
local main TREE

ahead/behind

working-tree status


Expected canonical GitHub identity:

SHA:
11eb7d0038f2d60b8540cc2f027d2b50c2117b91

TREE:
d3c230005c043874b9e245b70cd4b23f70af8105


If origin/main has advanced:

record the new SHA/tree and commits.

Do NOT modify history.

Do NOT use a newer commit to reinterpret historical run 35120305466.


==================================================
MISSION 2 — RECONFIRM STEP 38 FAILURE
==================================================

Re-query exact run:

35120305466


Verify:

workflow
workflow ID
run number
run attempt
event
branch
head SHA
status
conclusion
created time
started time
updated time
check-suite ID


Re-query:

jobs
attempt jobs
check suite
check runs
logs
annotations
timing/billable data


Confirm whether the Step 38 observations remain:

jobs = 0
check runs = 0
logs unavailable
annotations unavailable
runner allocation = none


If GitHub now exposes additional diagnostic evidence that was unavailable
during Step 38, capture it.


==================================================
MISSION 3 — INSPECT GITHUB ACTIONS REPOSITORY SETTINGS
==================================================

Read the repository's GitHub Actions configuration WITHOUT CHANGING IT.

Determine all observable values for:

- Actions enabled/disabled state;
- allowed actions policy;
- reusable workflow policy;
- GitHub-owned actions allowance;
- verified creator allowance;
- selected actions restrictions;
- workflow permissions;
- default GITHUB_TOKEN permission;
- whether workflows may create/approve pull requests;
- fork pull-request workflow policy where applicable;
- artifact/log retention;
- repository Actions access;
- organization-level policy inherited by this repository, if observable.


Specifically determine whether any repository or organization Actions policy
could reject the workflow BEFORE job creation.

Record API endpoint/status/evidence for every conclusion.


==================================================
MISSION 4 — WORKFLOW REGISTRATION / VALIDATION STATE
==================================================

Inspect GitHub's registered workflow metadata for:

.github/workflows/release-validation.yml


Record:

workflow ID
workflow API name
workflow path
workflow state
created_at
updated_at
badge/state if exposed


Determine whether GitHub considers it:

active
disabled
disabled_inactivity
invalid
or another state.


Compare the registered workflow metadata with the exact file at canonical main.

Confirm the registered workflow points to:

.github/workflows/release-validation.yml


Also inspect whether multiple historical workflow records exist for the same
path/name.


==================================================
MISSION 5 — GITHUB-SIDE WORKFLOW VALIDATION
==================================================

Use every available READ-ONLY GitHub diagnostic/API surface to determine
whether GitHub considers the workflow definition semantically valid.

Investigate especially:

permissions:
  contents: read
  administration: read


Determine whether:

administration: read

is a valid GitHub Actions workflow permission key in this repository/context.

DO NOT assume local YAML parsing proves GitHub semantic validity.


Inspect all workflow-level keys and job-level keys for GitHub semantic
acceptance.

If GitHub exposes a workflow validation error, capture the EXACT error text.

This is a HIGH-PRIORITY investigation.


==================================================
MISSION 6 — PERMISSIONS FORENSICS
==================================================

Audit the exact workflow permissions:

permissions:
  contents: read
  administration: read


Determine from authoritative GitHub behavior/API evidence whether each
permission is accepted in a workflow `permissions:` block.

Determine whether an unsupported permission key could cause:

workflow run created
→ zero jobs
→ immediate failure
→ no logs/check runs


Do NOT edit the permissions.

If `administration: read` is invalid or unsupported in Actions workflow syntax,
prove it rather than infer it.


Also determine what permission is actually needed for:

node scripts/audit-required-checks.mjs

to read branch-protection information.


Separate:

A. workflow syntax/semantic permission validity

from:

B. runtime token authorization to call the branch-protection API.


==================================================
MISSION 7 — COMPARE WORKFLOWS THAT RUN SUCCESSFULLY
==================================================

Identify workflows on the SAME canonical SHA/main push that successfully
created jobs/check runs.

Step 38 observed at least other workflow activity.

Select one or more successful/non-zero-job workflows and compare:

- top-level `on:`
- `permissions:`
- runner
- environment
- secrets
- action restrictions
- workflow state
- job syntax
- repository policy interaction.


The goal is to identify what differs BEFORE runner initialization.


Do not compare application behavior.

Compare controller-facing workflow structure.


==================================================
MISSION 8 — COMPARE ZERO-JOB FAILURES
==================================================

Investigate these exact same-push zero-job failures:

Release validation:
35120305466

Native auth preflight:
35120303752

Native encrypted recovery:
35120304682


For each record:

- workflow path
- workflow state
- event
- head SHA
- trigger declaration
- permissions block
- runner declaration
- environments
- secrets references
- jobs
- check runs
- logs
- timing
- annotations.


Find the structural intersection between all three zero-job failures.


Determine whether they share:

- invalid permission key;
- unsupported workflow syntax;
- repository policy;
- organization Actions restriction;
- environment restriction;
- runner-group restriction;
- workflow registration problem;
- event mismatch;
- required-workflow configuration;
- or another controller-level property.


==================================================
MISSION 9 — COMPARE SUCCESSFUL SAME-PUSH WORKFLOWS
==================================================

Inspect same-push workflows that DID initialize successfully, including where
available:

Account-deletion fence validation

Native association monitoring


Wait/re-query only as needed to obtain their completed state.

Do NOT rerun anything.


For successful/non-zero-job examples record:

job count
check-run count
runner
permissions
trigger
workflow path
workflow state


Then produce a structural comparison matrix:

PROPERTY
ZERO-JOB WORKFLOWS
WORKING WORKFLOWS
DIFFERENCE
ROOT-CAUSE RELEVANCE


==================================================
MISSION 10 — REPOSITORY RULESETS / REQUIRED WORKFLOWS
==================================================

Read all observable repository rulesets and branch protection WITHOUT CHANGES.

Determine:

- rulesets targeting main;
- rulesets targeting default branch;
- required workflows;
- required status checks;
- workflow restrictions;
- deployment requirements;
- bypass actors;
- integration IDs;
- organization-level inherited rules if observable.


Confirm whether any required-workflow/ruleset configuration could terminate
the release-validation workflow before job creation.


Do not confuse:

required STATUS CHECK

with:

required WORKFLOW.


Document both separately.


==================================================
MISSION 11 — ENVIRONMENT / DEPLOYMENT POLICY
==================================================

Determine whether release-validation references a GitHub Environment.

If no environment is referenced, state that.

If environments exist in the repository, do NOT modify them.

Determine whether environment protection can plausibly explain this specific
pre-job failure.


Also distinguish unrelated Railway deployment statuses from GitHub Actions
workflow execution.


==================================================
MISSION 12 — RUNNER / BILLING / ACTIONS AVAILABILITY
==================================================

Read available repository/account evidence for:

- Actions availability;
- billing/spending restrictions if observable;
- hosted-runner availability;
- runner groups;
- self-hosted runner availability;
- repository access to runner groups;
- suspended/disabled Actions state;
- concurrency limits if observable.


Important:

Release validation uses:

ubuntu-latest


Therefore determine whether GitHub-hosted jobs generally work in this
repository.


Native workflows use:

self-hosted, calora-native


Do not incorrectly attribute the Release validation zero-job failure to the
native self-hosted runner unless evidence proves a shared controller cause.


==================================================
MISSION 13 — AUDIT EXTERNAL ACTION POLICY
==================================================

Release validation references:

actions/checkout@v4
pnpm/action-setup@v4
actions/setup-node@v4


Determine whether repository/organization allowed-actions policy permits all
three.


If third-party actions are restricted, specifically determine whether:

pnpm/action-setup@v4

is allowed.


Again, determine whether policy rejection would occur before or after job
creation according to available evidence.


Do not modify allowed-actions policy.


==================================================
MISSION 14 — AUDIT WORKFLOW HISTORY / REGISTRATION
==================================================

Inspect historical workflow runs and workflow-file evolution.

Determine:

- when Release validation workflow was introduced;
- when GitHub first registered workflow ID 359784386;
- whether it ever produced a non-zero job;
- whether a prior version had different permissions;
- whether the workflow was disabled/re-enabled;
- whether GitHub metadata indicates stale registration.


Perform equivalent lightweight history inspection for the two native zero-job
workflows.


Look for a common introduction/refactor commit.


==================================================
MISSION 15 — GITHUB API ERROR SURFACES
==================================================

For run 35120305466 and check suite 95111026936, inspect every relevant
read-only diagnostic surface available.

Examples include:

workflow run
workflow attempt
jobs
check suite
check runs
annotations
logs
timing
workflow metadata
commit statuses
repository Actions settings
branch protection
rulesets


Record HTTP response codes.

Do NOT brute-force unrelated endpoints.

Do NOT mutate anything.


==================================================
MISSION 16 — DETERMINE WHETHER WORKFLOW FILE ITSELF IS INVALID
==================================================

At the end of the GitHub-side audit answer explicitly:

Does GitHub accept `.github/workflows/release-validation.yml` as a fully valid
executable workflow?

Answer one:

YES — PROVEN

NO — PROVEN INVALID

UNKNOWN — REGISTERED BUT EXECUTION VALIDITY NOT PROVEN


If NO:

provide the exact invalid field/expression and GitHub evidence.


==================================================
MISSION 17 — ROOT CAUSE
==================================================

Classify the PRIMARY controller failure into ONE:

A. INVALID GITHUB ACTIONS WORKFLOW SEMANTICS

B. INVALID WORKFLOW PERMISSION DECLARATION

C. REPOSITORY ACTIONS POLICY RESTRICTION

D. ORGANIZATION ACTIONS POLICY RESTRICTION

E. REQUIRED-WORKFLOW / RULESET CONFLICT

F. WORKFLOW REGISTRATION / STALE METADATA FAILURE

G. RUNNER / BILLING / ACTIONS AVAILABILITY FAILURE

H. EVENT / CONTROLLER SCHEDULING FAILURE

I. MULTIPLE CONTROLLER CONFIGURATION FAILURES

J. GITHUB PLATFORM / INTERNAL FAILURE

K. INSUFFICIENT EVIDENCE


Then state confidence:

HIGH
MEDIUM
LOW


Do not choose a category simply because it seems likely.

Evidence must support it.


==================================================
MISSION 18 — MINIMUM REMEDIATION DESIGN
==================================================

ONLY after root cause analysis, design the smallest remediation.

DO NOT APPLY IT.


Specify:

- exact file or GitHub setting;
- exact line/property;
- current value;
- proposed value;
- why it addresses the proven failure;
- whether new canonical commit required;
- whether branch protection changes;
- whether Actions settings changes;
- whether organization-admin action required;
- whether secrets change;
- whether runner change;
- whether database/application/native code remains untouched.


If evidence is insufficient:

DO NOT invent a remediation.

State what additional evidence or owner-side GitHub UI observation is required.


==================================================
MISSION 19 — OWNER-UI EVIDENCE IF API IS INSUFFICIENT
==================================================

If GitHub's API still hides the controller failure reason, identify the exact
GitHub UI location the owner should inspect.

Be extremely specific.

For example, identify:

Repository
→ Actions
→ exact workflow
→ exact run
→ exact banner/error area

or:

Repository
→ Settings
→ Actions
→ General
→ exact setting


State exactly what text/error/status the owner should capture.

Do NOT ask the owner to change anything.

Only request evidence if it is genuinely unavailable programmatically.


==================================================
MISSION 20 — APPLICATION / TESTFLIGHT IMPACT
==================================================

State explicitly whether the evidence implicates:

- Calora application code;
- production API;
- database;
- iOS binary;
- TestFlight;
- Expo/EAS.


Do not infer any native failure from a zero-job GitHub controller failure.


==================================================
MISSION 21 — CONFIRM NO MUTATIONS
==================================================

Confirm:

workflow edits: none
GitHub settings edits: none
branch protection edits: none
ruleset edits: none
secret changes: none
variable changes: none
runner changes: none
commits: no remediation commit
push: none
workflow rerun: none
workflow dispatch: none
EAS: none
iOS build: none
Android build: none
TestFlight: none
API deployment: none
Replit publish: none
database mutation: none
migration: none


==================================================
OUTPUT REPORT
==================================================

Create:

39_CALORA_GITHUB_ACTIONS_CONTROLLER_FORENSIC_REPORT.md


The report MUST contain:

1. Executive summary
2. Canonical SHA/tree
3. Current local/origin state
4. Step 38 run reconfirmation
5. Repository Actions settings
6. Workflow registration state
7. GitHub-side workflow validity evidence
8. Workflow permissions analysis
9. `administration: read` validity analysis
10. Required branch-protection context
11. Repository rulesets
12. Required-workflow analysis
13. Zero-job workflow comparison
14. Successful same-push workflow comparison
15. Structural comparison matrix
16. Environment/deployment policy
17. Runner/billing/Actions availability
18. Allowed-actions policy
19. Workflow history/registration
20. API diagnostic response matrix
21. Exact first observable controller failure
22. Whether workflow is proven valid/invalid/unknown
23. Primary root-cause classification
24. Root-cause confidence
25. Secondary findings
26. Minimum remediation design
27. Exact files/settings implicated
28. Whether canonical SHA must change
29. Whether owner GitHub UI evidence is required
30. Exact owner UI inspection instructions if required
31. Application/TestFlight impact
32. Confirmation no mutations occurred
33. Exact recommendation for Step 40


FINAL VERDICT MUST BE EXACTLY ONE OF:

CONTROLLER ROOT CAUSE IDENTIFIED — REMEDIATION READY FOR OWNER AUTHORIZATION

CONTROLLER ROOT CAUSE PARTIALLY IDENTIFIED — OWNER GITHUB EVIDENCE REQUIRED

CONTROLLER ROOT CAUSE NOT IDENTIFIED — DO NOT MODIFY CI


==================================================
STOP CONDITION
==================================================

STOP after diagnostic investigation and numbered report.

DO NOT REMEDIATE.

DO NOT PUSH.

DO NOT RERUN WORKFLOWS.

DO NOT DISPATCH WORKFLOWS.

DO NOT BUILD IOS.

DO NOT BUILD ANDROID.

DO NOT TRIGGER EAS.

DO NOT SUBMIT TESTFLIGHT.

DO NOT DEPLOY API.

DO NOT MODIFY DATABASE.

Wait for explicit owner authorization for Step 40.