CALORA — STEP 36 CANONICAL MAIN RELEASE GATE + TESTFLIGHT BUILD

OUTPUT REPORT:

36_CALORA_CANONICAL_MAIN_TESTFLIGHT_BUILD_REPORT.md


==================================================
OWNER AUTHORIZATION
==================================================

Step 35 completed with:

FINAL GITHUB MAIN PUSH VERIFIED — CANONICAL MAIN SYNCHRONIZED


Canonical GitHub main was frozen as:

SHA:
8d93248db69e985ba490a957575bde2b7b38cbf3

TREE:
0a0ac2b83abce082bbc870c58029376cc4541a4a


I now authorize:

1. verification of the GitHub release-validation result for this exact SHA;

AND, ONLY IF THAT GATE PASSES:

2. one new iOS production/TestFlight build from this exact canonical main;

3. submission of that exact successful iOS build to TestFlight.


This authorization DOES NOT include:

- Android build
- production API deployment
- Replit production publish
- database changes
- migrations
- branch reconciliation
- source-code feature changes
- force push
- Git history rewriting


==================================================
MISSION 1 — RE-FREEZE CANONICAL MAIN
==================================================

Fetch origin without modifying history.

Verify:

origin/main SHA

must equal:

8d93248db69e985ba490a957575bde2b7b38cbf3


Verify tree:

0a0ac2b83abce082bbc870c58029376cc4541a4a


Verify:

local main == origin/main

ahead = 0
behind = 0

working tree clean.


If origin/main has advanced:

STOP.

DO NOT BUILD.

Create the Step 36 report with a BLOCKED verdict and identify the new commits.


==================================================
MISSION 2 — VERIFY GITHUB RELEASE VALIDATION
==================================================

Inspect GitHub Actions/check status for EXACT SHA:

8d93248db69e985ba490a957575bde2b7b38cbf3


Specifically verify the required check:

Run release validation suite


Record:

- workflow/run name
- workflow run ID
- triggering event
- branch
- commit SHA
- start time
- completion time
- conclusion
- failed/skipped steps if applicable


REQUIRED RESULT:

SUCCESS


If the required release validation:

- failed
- was cancelled
- is missing
- cannot be proven
- applies to a different SHA
- is still pending

DO NOT BUILD.

STOP with:

CANONICAL RELEASE GATE NOT VERIFIED — TESTFLIGHT BUILD NOT STARTED


Do not bypass the check.


==================================================
MISSION 3 — PRE-BUILD SOURCE VERIFICATION
==================================================

Only after Mission 2 passes:

Verify the exact build source contains the reconciled functionality:

1. durable capture acceptance;

2. Weekly Programs deterministic modal-state transition;

3. shared Planner Program pools and typed eligibility;

4. bounded Coach request lifecycle;

5. Coach Fact Context restricted to:

daily.calorie_status
daily.protein_status

6. diary image provenance/imageAssetKey sync;

7. capture compatibility assertion.


Verify preservation of:

- recipe nextOffset
- recipe terminalReason
- capture approval security
- PKCE/auth
- account isolation
- deletion fences
- sync ownership
- release attestation
- Expo Router entry
- production EAS build profile
- production EAS submit profile
- Apple Health privacy-purpose strings
- bundle identifier com.etiendem.caloraapp
- Expo project identity
- TestFlight workflow


==================================================
MISSION 4 — PRE-BUILD VALIDATION
==================================================

Run the deterministic release checks required for this exact SHA.

At minimum:

pnpm run typecheck

pnpm --filter @workspace/api-server test

pnpm --filter @workspace/calora test

release-attestation validation

Expo configuration validation

EAS configuration validation

Apple Health privacy configuration validation

generated API contract compatibility validation

git diff --check


If anything fails:

DO NOT BUILD.

STOP and report the failure.

Do not modify source merely to force the build through.


==================================================
MISSION 5 — BUILD IDENTITY
==================================================

Before triggering EAS, record:

BUILD SOURCE SHA:
8d93248db69e985ba490a957575bde2b7b38cbf3

BUILD SOURCE TREE:
0a0ac2b83abce082bbc870c58029376cc4541a4a

PROJECT:
Calora

PLATFORM:
iOS

PROFILE:
production

PROJECT DIRECTORY:
artifacts/calora

BUNDLE ID:
com.etiendem.caloraapp


Verify the build is using the existing linked Expo/EAS project.

Do not create a second Expo project.


==================================================
MISSION 6 — IOS BUILD NUMBER
==================================================

Determine the currently accepted App Store Connect/TestFlight build number.

The previous known TestFlight build was:

1.0.0 (2)


The new submission MUST use a valid higher build number.

Do not overwrite or reuse build number 2.

Use the repository/EAS build-number mechanism already established by the
current canonical configuration.

Record the final:

marketing version

iOS build number


Do not change unrelated application versioning.


==================================================
MISSION 7 — TRIGGER EXACT IOS PRODUCTION BUILD
==================================================

Trigger ONE iOS production EAS build from the exact canonical source.

Equivalent intended operation:

working directory:
artifacts/calora

platform:
ios

profile:
production


Do not trigger Android.

Do not trigger multiple iOS builds.

Do not switch branches.

Do not build from release/calora-onboarding-and-plus.

Do not build from the isolated reconciliation branch.

ONLY canonical main is authorized.


==================================================
MISSION 8 — VERIFY EAS BUILD
==================================================

Wait for the iOS build to complete.

Record:

- EAS build ID
- project ID
- Git commit SHA
- Git tree/source identity where available
- platform
- profile
- app version
- iOS build number
- bundle identifier
- start time
- completion time
- final status


Required status:

FINISHED / SUCCESS


Verify the EAS build record identifies the canonical SHA:

8d93248db69e985ba490a957575bde2b7b38cbf3


If the build points to another Git SHA:

DO NOT SUBMIT IT.

STOP.


==================================================
MISSION 9 — TESTFLIGHT SUBMISSION
==================================================

Only after the exact build succeeds:

Submit THAT EXACT EAS BUILD to App Store Connect/TestFlight.

Do not use an ambiguous historical build selection.

Prefer explicit build identity when supported.

If the workflow uses --latest, first prove that the exact newly created
canonical build is the latest eligible iOS production build.


Record:

- EAS submission ID
- EAS build ID being submitted
- bundle identifier
- version
- build number
- App Store Connect app identity
- submission start
- submission completion
- submission status


Required result:

SUCCESS


==================================================
MISSION 10 — APP STORE CONNECT / TESTFLIGHT VERIFICATION
==================================================

After successful submission, verify that App Store Connect received the new
build.

Record:

- version
- build number
- processing/testing state
- internal testing availability if observable


Do not claim physical-device success.

Submission success and device behavior are separate.


==================================================
MISSION 11 — PRODUCTION API BOUNDARY
==================================================

DO NOT DEPLOY THE API.

DO NOT PUBLISH REPLIT PRODUCTION.

DO NOT CHANGE DATABASE.

The currently verified production API remains untouched.

This mission is strictly:

GitHub release gate
→ canonical iOS build
→ TestFlight submission.


==================================================
MISSION 12 — FAILURE HANDLING
==================================================

If GitHub release validation fails:

STOP before EAS.


If EAS build fails:

DO NOT automatically edit source.

DO NOT create repeated builds.

Record the exact failure.


If TestFlight submission fails:

DO NOT rebuild automatically unless the binary itself is proven invalid.

Preserve the successful EAS build identity and report the submission failure.


If any source SHA mismatch occurs:

STOP immediately.


==================================================
MISSION 13 — FINAL REPORT
==================================================

Create:

36_CALORA_CANONICAL_MAIN_TESTFLIGHT_BUILD_REPORT.md


The report MUST contain:

1. Executive summary
2. Canonical GitHub main SHA/tree
3. Local/origin synchronization verification
4. GitHub release-validation workflow result
5. Required status-check result
6. Pre-build validation results
7. Exact build source identity
8. Expo/EAS project identity
9. iOS bundle identifier
10. Marketing version
11. iOS build number
12. EAS build ID
13. EAS build result
14. EAS Git SHA verification
15. TestFlight submission ID
16. Submitted EAS build ID
17. App Store Connect submission result
18. TestFlight processing/testing state
19. Weekly Programs fix presence in source
20. Coach/capture/planner/diary reconciliation verification
21. Confirmation production API was untouched
22. Confirmation database/migrations were untouched
23. Confirmation Android was not built
24. Remaining device-verification requirements
25. Exact instructions for owner physical iOS test
26. Exact recommendation for next step


FINAL VERDICT MUST BE EXACTLY ONE OF:

CANONICAL IOS BUILD SUBMITTED — READY FOR TESTFLIGHT DEVICE VERIFICATION

CANONICAL RELEASE GATE NOT VERIFIED — TESTFLIGHT BUILD NOT STARTED

CANONICAL IOS BUILD FAILED — TESTFLIGHT NOT SUBMITTED

CANONICAL TESTFLIGHT SUBMISSION FAILED — BUILD PRESERVED


==================================================
DEVICE TEST PLAN FOR OWNER
==================================================

After the new build becomes available in TestFlight, instruct the owner to:

1. Update/install the NEW Calora TestFlight build.

2. Confirm the displayed build number is the newly submitted build.

3. Open Calora.

4. Open Plan.

5. Tap the gear icon.

6. Open Weekly Programs.

7. Tap one of the programs.

EXPECTED:

The selected Program detail opens immediately.

8. Close/back.

EXPECTED:

Returns correctly without a stuck overlay.

9. Select another Program.

EXPECTED:

Its detail opens.

10. Tap Apply.

EXPECTED:

Program applies and the modal closes normally.

11. Repeat once more to verify there is no intermittent touch failure.


Also perform a short smoke test:

- Home
- Recipes
- Smart Scan
- Coach
- Diary/Profile
- authentication/session persistence


==================================================
STOP CONDITION
==================================================

STOP after:

- GitHub release gate verification;
- one successful canonical iOS build, if authorized gate passes;
- submission of that exact build to TestFlight;
- numbered report.

DO NOT BUILD ANDROID.

DO NOT DEPLOY API.

DO NOT PUBLISH REPLIT PRODUCTION.

DO NOT MODIFY DATABASE.

DO NOT MAKE NEW SOURCE CHANGES.

Wait for owner physical TestFlight verification.