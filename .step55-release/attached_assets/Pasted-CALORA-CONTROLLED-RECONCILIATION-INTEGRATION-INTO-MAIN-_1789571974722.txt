CALORA — CONTROLLED RECONCILIATION INTEGRATION INTO MAIN

THIS IS STEP 33.

SOURCE RECONCILIATION BRANCH:

reconcile/calora-onboarding-plus-controlled

HISTORICAL SOURCE BRANCH — READ ONLY:

release/calora-onboarding-and-plus


REFERENCE REPORT:

32_CALORA_TARGETED_BRANCH_RECONCILIATION_REMEDIATION_REPORT.md


==================================================
OWNER AUTHORIZATION
==================================================

The controlled reconciliation completed successfully and received:

CONTROLLED RECONCILIATION COMPLETE — READY FOR OWNER REVIEW

I now authorize integration of the VALIDATED CONTROLLED RECONCILIATION
into LOCAL canonical main.

This authorization applies ONLY to the reconciled/validated work.

DO NOT merge the historical branch:

release/calora-onboarding-and-plus

into main.

The historical source branch must remain untouched.


==================================================
AUTHORIZED RECONCILIATION COMMITS
==================================================

The validated reconciliation report identified these functional commits:

d5edd547c42ee7e5990bafdea7320bcf40eebf9
Reconcile durable capture acceptance

d9fb18310fc366e78b721e30fc9c771fdad0a5b3
Fix weekly program modal transition

37ac5ab351255f67ad3d5960ed7d62bd87f0d66e
Reconcile shared planner program rules

6ae455e51aea96c6305703943c671c72c4d9eb9e
Harden bounded Coach fact requests

b1d354bf7003cba6ca861812dba1fcc6ee692750
Preserve diary image provenance across sync

774f289ed6d39cbec8b19f4a2e7a01cabb5d46f9
Align capture compatibility assertion


These are the authorized functional reconciliation units.

Do not substitute historical source commits for them.


==================================================
CRITICAL STOP CONDITIONS
==================================================

Before modifying main:

fetch/refresh relevant refs.

Determine the EXACT current:

local main SHA/tree
origin/main SHA/tree
reconciliation branch SHA/tree

Compare current local main against the reconciliation branch's recorded base:

08f35a532cd766f9fd3e93f874dd83066461b4e4
tree 61c3b08ed8cd6034143f3476c6717969ce3d9048


If local main has advanced:

DO NOT blindly reset it.

Classify every new main commit since the frozen base.

If the advancement contains only:

- reconciliation reports
- authorization evidence
- agent metadata
- other non-runtime documentation

then preserve that material and continue only if the functional reconciliation
can be integrated without changing runtime meaning.

If main contains ANY new:

- application runtime work
- API work
- database work
- security work
- native configuration
- Expo/EAS configuration
- release configuration
- generated contract changes

that were not considered by Step 32:

STOP.

Do not integrate.

Produce the Step 33 report with a BLOCKED verdict explaining exactly what
changed.


==================================================
DO NOT
==================================================

DO NOT:

- merge release/calora-onboarding-and-plus
- perform a blind whole-branch merge
- force-push
- rewrite published Git history
- reset away valid main commits
- push GitHub
- deploy production
- modify production
- modify any database
- run drizzle-kit push
- run migrations
- trigger GitHub production workflows
- trigger EAS
- build Android
- build iOS
- submit TestFlight
- submit App Store

THIS STEP ENDS WITH LOCAL MAIN ONLY.


==================================================
MISSION 1 — VERIFY RECONCILIATION BRANCH
==================================================

Verify that:

reconcile/calora-onboarding-plus-controlled

still contains the six authorized reconciliation commits.

Verify its runtime tree has not changed unexpectedly since Step 32.

Confirm all six commits are reachable and in the expected order/dependency
structure.

Inspect any commits after the recorded Step 32 functional tip.

Classify them as:

RUNTIME
TEST
CONFIGURATION
REPORT/EVIDENCE
UNEXPECTED

No unexpected runtime/configuration change may enter main.


==================================================
MISSION 2 — ESTABLISH ROLLBACK CHECKPOINT
==================================================

Immediately before integration, record:

PRE-INTEGRATION MAIN SHA
PRE-INTEGRATION MAIN TREE

This becomes the exact rollback checkpoint.

Do not delete or rewrite it.

All integration must use normal auditable Git history.


==================================================
MISSION 3 — INTEGRATE ONLY VALIDATED WORK
==================================================

Integrate the six validated reconciliation units into LOCAL main.

Use the safest Git method based on the actual topology.

Acceptable methods include:

- fast-forward if and only if topology proves it safe;
- selective cherry-pick of the six validated commits;
- controlled merge of the isolated reconciliation branch if and only if the
  resulting tree is proven equivalent to the validated Step 32 runtime tree
  plus preserved non-runtime main documentation.

DO NOT merge the historical source branch.

DO NOT resolve conflicts by automatically choosing "ours" or "theirs".

If a conflict occurs:

inspect it semantically.

Protect current main invariants.

If conflict resolution changes the already-validated runtime semantics:

STOP and revalidate that functional unit before proceeding.


==================================================
MISSION 4 — PROTECT MAIN INVARIANTS
==================================================

After integration, explicitly prove preservation of:

CAPTURE

- POST /api/v1/capture/:sessionId/approve
- UUID validation
- authentication
- owner scoping
- idempotency
- deletion fence
- expected HTTP semantics

RECIPES / PREMIUM

- nextOffset
- terminalReason
- authentication
- entitlement
- rate limiting
- provider failure behavior

COACH

Fact Context remains restricted to:

daily.calorie_status
daily.protein_status

Preserve:

- consent
- validation
- authorization
- deletion revalidation
- lifecycle protections
- generated contract alignment

AUTH / ACCOUNT

Preserve:

- PKCE
- session restoration
- account isolation
- persistence boundaries
- account deletion fences
- sync ownership

RELEASE / NATIVE

Preserve current:

- Expo Router entry point
- artifacts/calora working directory
- pnpm/xmldom remediation
- image-size/EAS archive remediation
- eas submit.production
- Apple Health privacy-purpose strings
- GitHub TestFlight workflow
- release validation
- release attestation
- native association monitoring


==================================================
MISSION 5 — VERIFY RECOVERED FUNCTIONALITY
==================================================

Confirm local main now contains the validated Step 32 behavior:

1. Durable capture acceptance sequencing/persistence.

2. Weekly Programs modal state-machine fix.

Required state behavior:

selector
→ select program
→ detail
→ close/back correctly
→ Apply closes correctly

No nested native modal dependency.

3. Shared Planner Program pools and typed eligibility.

4. Bounded Coach request lifecycle:

- timeout
- cancellation
- malformed response handling
- stale request protection

without broadening Coach fact policy.

5. Diary image provenance/imageAssetKey sync behavior.

6. Capture compatibility assertion aligned with durable acceptance.


==================================================
MISSION 6 — DATABASE SAFETY
==================================================

No database change is authorized.

Verify:

- migration files unchanged
- migration order unchanged
- no duplicate migration
- no schema change
- no drizzle-kit push
- no DB mutation

The image provenance work must continue using the existing sync metadata
boundary as validated in Step 32.


==================================================
MISSION 7 — FULL POST-INTEGRATION VALIDATION
==================================================

Now run the complete NON-DEPLOYING validation against LOCAL MAIN.

At minimum:

pnpm --filter @workspace/api-server test

pnpm --filter @workspace/calora test

full workspace:

pnpm run typecheck

plus focused validation for:

- capture
- capture persistence
- capture approval
- diary sync
- Planner
- Planner program eligibility
- Weekly Programs modal state
- Recipes
- Premium Recipes
- Coach
- Coach Fact Context
- authentication
- account isolation
- deletion fences
- sync
- image metadata/provenance
- release attestation
- Expo configuration
- native auth preflight
- generated API contracts

Run:

git diff --check

Verify package/lockfile consistency.

Verify no generated contract drift.

Perform safe/non-mutating native/prebuild checks where appropriate.

DO NOT perform an EAS build.


==================================================
MISSION 8 — TREE EQUIVALENCE AUDIT
==================================================

Compare:

A. Step 32 validated reconciliation branch runtime tree

against

B. post-integration local main runtime tree

Account separately for legitimate:

- reports
- authorization evidence
- metadata

The application/API/security/configuration behavior should be equivalent to
the validated reconciliation result unless an explicitly documented,
revalidated conflict resolution was necessary.

Produce a changed-file manifest from:

PRE-INTEGRATION MAIN

to:

POST-INTEGRATION MAIN

Every changed runtime/test/config file must map to one of the six authorized
reconciliation units.

NO unexplained runtime changes are allowed.


==================================================
MISSION 9 — IOS WEEKLY PROGRAM STATIC VERIFICATION
==================================================

Specifically verify the integrated main contains the Step 32 modal-state
remediation.

Confirm source structure no longer depends on presenting the Program detail
as a second nested native modal over the Program selector.

Confirm regression tests pass.

IMPORTANT:

Do NOT claim physical iOS device success yet.

Actual device confirmation requires a later authorized iOS/TestFlight build.

Report this as:

STATIC/TEST VERIFIED — DEVICE VERIFICATION PENDING

if repository validation succeeds.


==================================================
MISSION 10 — FINAL LOCAL MAIN FREEZE
==================================================

If and ONLY if every required validation passes:

record:

POST-INTEGRATION MAIN SHA
POST-INTEGRATION MAIN TREE

Confirm working tree clean.

Do NOT push it.

Do NOT deploy it.

Do NOT build it.

This exact SHA/tree becomes the candidate for the NEXT owner authorization.


==================================================
FAILURE HANDLING
==================================================

If integration or validation fails:

DO NOT weaken security or compatibility requirements.

DO NOT push partial work.

Either:

A. repair the isolated integration safely and rerun the complete affected
validation;

or

B. return local main through normal safe Git operations to the recorded
PRE-INTEGRATION MAIN state.

Do not force-push or rewrite published history.

Document exactly what occurred.


==================================================
NUMBERED OUTPUT REPORT
==================================================

Create:

33_CALORA_CONTROLLED_RECONCILIATION_MAIN_INTEGRATION_REPORT.md


The report MUST include:

1. Executive summary
2. Pre-integration local main SHA/tree
3. origin/main identity
4. Reconciliation branch identity
5. Advancement analysis since Step 32
6. Integration method used
7. Exact commits integrated
8. Conflict resolutions, if any
9. Capture invariant verification
10. Planner reconciliation verification
11. iOS Weekly Programs static/test verification
12. Coach policy/lifecycle verification
13. Diary/image-sync verification
14. Recipes/Premium compatibility verification
15. Auth/security/account verification
16. Release/native configuration verification
17. Database/migration verification
18. Full test results
19. Typecheck results
20. Generated-contract verification
21. Final changed-file manifest
22. Tree-equivalence analysis
23. Pre-integration rollback identity
24. Post-integration main SHA/tree
25. Working-tree cleanliness
26. Confirmation that nothing was pushed/deployed/built
27. Remaining uncertainties
28. Exact recommendation for next owner-authorized step


FINAL VERDICT MUST BE EXACTLY ONE OF:

LOCAL MAIN INTEGRATION VERIFIED — READY FOR OWNER PUSH REVIEW

LOCAL MAIN INTEGRATION BLOCKED — MAIN PRESERVED

LOCAL MAIN INTEGRATION FAILED — ROLLED BACK SAFELY


==================================================
STOP CONDITION
==================================================

STOP after:

- local integration,
- complete validation,
- numbered report,
- final SHA/tree recording.

DO NOT PUSH GITHUB.
DO NOT DEPLOY.
DO NOT TRIGGER EAS.
DO NOT BUILD ANDROID.
DO NOT BUILD IOS.
DO NOT SUBMIT TESTFLIGHT.

Wait for explicit owner authorization.