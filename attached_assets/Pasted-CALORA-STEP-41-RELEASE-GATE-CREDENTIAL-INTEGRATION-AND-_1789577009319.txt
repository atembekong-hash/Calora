CALORA — STEP 41 RELEASE GATE CREDENTIAL INTEGRATION AND VERIFIED EXECUTION

OUTPUT REPORT:

41_CALORA_RELEASE_GATE_CREDENTIAL_INTEGRATION_AND_VERIFICATION_REPORT.md


==================================================
OWNER AUTHORIZATION
==================================================

Step 40 ended with:

PERMISSION SYNTAX FIX IDENTIFIED — SUPPORTED AUDIT CREDENTIAL REQUIRED


The owner has now manually created a GitHub fine-grained personal access token
scoped ONLY to:

atembekong-hash/Calora


The token has:

Repository Administration:
READ-ONLY

Metadata:
READ-ONLY (GitHub-required)


The owner stored the token as this GitHub Actions repository secret:

CALORA_RELEASE_GATE_ADMIN_READ_TOKEN


The secret value MUST NEVER be:

- printed;
- echoed;
- logged;
- returned through GitHub API;
- written to a report;
- written to a file;
- committed;
- copied into source;
- exposed through shell tracing;
- exposed through debugging output.


I authorize Step 41 to perform ONLY the minimum release-gate credential
integration and verification necessary to make the existing release-validation
gate executable and meaningful.


AUTHORIZED:

1. preserve the Step 40 removal of invalid:

   administration: read

2. use the existing GitHub repository secret:

   CALORA_RELEASE_GATE_ADMIN_READ_TOKEN

   as the read credential for the live required-status-check audit;

3. modify only the minimum workflow credential wiring necessary;

4. validate locally;

5. create a narrowly scoped remediation commit;

6. push normally to main after all safety checks;

7. observe the automatically triggered Release validation workflow;

8. verify the exact required check executes successfully;

9. stop before any mobile build, EAS operation, TestFlight submission,
   App Store submission, API deployment, Replit publish, or database change.


NOT AUTHORIZED:

- creating another PAT;
- reading or exposing the PAT value;
- changing the PAT permissions;
- changing repository secrets;
- changing branch protection;
- removing the required check;
- renaming the required check;
- weakening the audit;
- bypassing authentication failures;
- broadening GITHUB_TOKEN permissions;
- application changes;
- API changes;
- database changes;
- schema/migration changes;
- Expo/EAS configuration changes;
- native configuration changes;
- iOS build;
- Android build;
- TestFlight;
- App Store submission;
- production deployment;
- force push;
- rebase/reset/history rewriting.


==================================================
MISSION 1 — FREEZE CURRENT REMOTE STATE
==================================================

Fetch origin read-only.

Record:

origin/main SHA
origin/main TREE

local main SHA
local main TREE

ahead/behind
working-tree status


Expected remote canonical identity from Step 40:

SHA:
11eb7d0038f2d60b8540cc2f027d2b50c2117b91

TREE:
d3c230005c043874b9e245b70cd4b23f70af8105


Step 40 reported local documentation/evidence commits plus one uncommitted
workflow edit removing:

administration: read


Classify all local-only commits and all working-tree changes.


If origin/main has advanced unexpectedly:

STOP.

DO NOT PUSH.

Final verdict:

RELEASE GATE CREDENTIAL INTEGRATION BLOCKED — REMOTE MAIN CHANGED


==================================================
MISSION 2 — VERIFY THE SECRET EXISTS SAFELY
==================================================

Verify through GitHub repository metadata/API only that a repository Actions
secret named EXACTLY:

CALORA_RELEASE_GATE_ADMIN_READ_TOKEN

exists.


DO NOT attempt to retrieve its value.

DO NOT print its value.

DO NOT test it outside GitHub Actions.

DO NOT replace it.

DO NOT recreate it.

DO NOT rename it.


Record only:

secret name
existence: yes/no
scope: repository secret if observable
updated/created metadata if safely observable


If the secret does not exist:

STOP.

Final verdict:

RELEASE GATE CREDENTIAL MISSING — NOT PUSHED


==================================================
MISSION 3 — PRESERVE THE PROVEN SYNTAX FIX
==================================================

The workflow:

.github/workflows/release-validation.yml

must NOT contain:

administration: read


Preserve:

permissions:
  contents: read


Do not add unsupported permission keys.

Do not add write-all.

Do not add unnecessary write permissions.


==================================================
MISSION 4 — PRESERVE RELEASE GATE IDENTITY
==================================================

Verify and preserve:

Workflow name:
Release validation

Job ID:
release-validation

Job display name:
Run release validation suite

Runner:
ubuntu-latest

Required branch-protection context:
Run release validation suite


Triggers must remain:

push:
  branches:
    - main

pull_request:

workflow_dispatch:


Do NOT modify branch protection.


==================================================
MISSION 5 — PRESERVE THE AUDIT'S SECURITY MODEL
==================================================

The existing script:

scripts/audit-required-checks.mjs

must remain fail-closed.


Its live protected-branch endpoint is:

GET /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks


It compares live required status contexts against workflow check names.


Preserve:

- live GitHub API inspection;
- required-context comparison;
- missing-context failure;
- API authorization failure;
- non-zero exit on failure.


Forbidden:

- hard-coded success;
- source-only replacement;
- exit 0 bypass;
- continue-on-error;
- ignoring 401;
- ignoring 403;
- converting audit failures to warnings;
- deleting the audit.


==================================================
MISSION 6 — MINIMUM CREDENTIAL WIRING
==================================================

Wire the existing repository secret ONLY into the required-check audit step.

Preferred minimum pattern:

env:
  GITHUB_TOKEN: ${{ secrets.CALORA_RELEASE_GATE_ADMIN_READ_TOKEN }}


Use the script's existing GITHUB_TOKEN interface unless inspection proves a
different already-supported input is required.


DO NOT expose the secret at:

workflow level

or unrelated steps.


Credential scope should be limited to the audit step wherever GitHub Actions
syntax permits.


The checkout step and other actions should continue using the normal Actions
authentication behavior unless there is a proven reason otherwise.


DO NOT configure checkout to use the PAT.

DO NOT persist the PAT into git credentials.

DO NOT pass the PAT to pnpm, Node, tests, EAS, Expo, deployment tooling, or
application processes.


==================================================
MISSION 7 — SECRET-EXPOSURE AUDIT
==================================================

Before commit, inspect the workflow and scripts for any possibility of secret
exposure.


Verify:

- no `echo $GITHUB_TOKEN`;
- no `printenv`;
- no `env` dump;
- no `set -x` around the audit;
- no shell debug tracing that exposes headers;
- no token in command-line arguments if avoidable;
- no token in report output;
- no token in generated files;
- no token in git diff;
- no token in git history;
- no token in application environment;
- no token forwarded to subsequent steps.


The report must NEVER include the secret value.


==================================================
MISSION 8 — CHECK THE LOCAL DIFF
==================================================

Expected functional change should remain extremely small.


Authorized functional path:

.github/workflows/release-validation.yml


Do not modify:

scripts/audit-required-checks.mjs

unless a modification is absolutely necessary to use its already-supported
credential interface.


Step 40 established that the script already accepts:

GITHUB_TOKEN || GH_TOKEN


Therefore prefer ZERO script modification.


Expected conceptual workflow change:

BEFORE:

permissions:
  contents: read
  administration: read

...

env:
  GITHUB_TOKEN: ${{ github.token }}


AFTER:

permissions:
  contents: read

...

env:
  GITHUB_TOKEN: ${{ secrets.CALORA_RELEASE_GATE_ADMIN_READ_TOKEN }}


Do not mechanically replace unrelated `${{ github.token }}` references.


==================================================
MISSION 9 — STATIC VALIDATION
==================================================

Before committing run all appropriate validations.


Verify:

YAML parsing
Prettier formatting
supported Actions permission keys
push-main trigger
pull_request trigger
workflow_dispatch trigger
workflow name
job ID
job display name
runner
timeout
checkout
audit command
test command
secret reference syntax


Run:

pnpm --filter @workspace/scripts test

pnpm run typecheck

git diff --check


Expected scripts suite baseline:

47 tests passed
0 failed


All validations must pass.


==================================================
MISSION 10 — APPLICATION SAFETY AUDIT
==================================================

Confirm there are NO functional changes under:

artifacts/calora/
artifacts/api-server/
lib/db/
lib/api-spec/
lib/api-client-react/
lib/api-zod/


Confirm no changes to:

Expo configuration
EAS configuration
native configuration
production API configuration
database schema
migrations
application runtime behavior


If unrelated functional changes appear:

STOP.

DO NOT PUSH.


==================================================
MISSION 11 — BRANCH-PROTECTION RECONFIRMATION
==================================================

Read-only verify immediately before commit/push:

main required status context is EXACTLY:

Run release validation suite


Do not modify it.


Confirm the workflow's job display name remains exactly identical.


==================================================
MISSION 12 — CREATE NARROW REMEDIATION COMMIT
==================================================

Only after all previous gates pass.


Create a narrowly scoped commit containing the release-gate remediation.


Suggested subject:

Fix release gate audit authentication


Record:

commit SHA
tree SHA
parent SHA
changed paths


The commit must contain no secret value.


Documentation-only local commits may remain in ancestry if already intentionally
present, but classify them explicitly.


==================================================
MISSION 13 — PRE-PUSH REMOTE RACE CHECK
==================================================

Fetch origin immediately before push.


Verify:

origin/main still equals the expected base or otherwise has not advanced
unexpectedly.

candidate is based on current origin/main ancestry.

behind = 0.

working tree clean.


If remote advanced unexpectedly:

STOP.

DO NOT PUSH.


Final verdict:

RELEASE GATE CREDENTIAL INTEGRATION BLOCKED — REMOTE MAIN CHANGED


==================================================
MISSION 14 — NORMAL PUSH
==================================================

If all safety gates pass:

git push origin main


STRICTLY FORBIDDEN:

--force
--force-with-lease
rebase
reset
history rewriting


Do NOT manually dispatch Release validation.


The push itself must naturally trigger:

Release validation


==================================================
MISSION 15 — IDENTIFY THE EXACT AUTOMATIC RUN
==================================================

Find the Release validation run created automatically from this push.


Record:

workflow ID
workflow run ID
run number
attempt
event
branch
head SHA
status
conclusion
created time
started time
completed time
check suite ID


The head SHA MUST equal the new canonical GitHub main SHA.


Do not accidentally inspect an older run.


==================================================
MISSION 16 — PROVE THE CONTROLLER PROBLEM IS FIXED
==================================================

Verify the new run DOES NOT show:

Unexpected value 'administration'


Verify:

job count > 0
check-run count > 0
billable/runner allocation exists as appropriate


A workflow run object alone is NOT sufficient.


==================================================
MISSION 17 — VERIFY REQUIRED CHECK
==================================================

There must be a check run named EXACTLY:

Run release validation suite


Record:

check-run ID
name
head SHA
status
conclusion
started time
completed time


Head SHA MUST equal the new canonical SHA.


==================================================
MISSION 18 — VERIFY STEP EXECUTION
==================================================

Verify each workflow step actually initializes and executes.


Expected sequence includes:

1. Checkout
2. Audit required checks
3. Set up pnpm
4. Set up Node.js
5. Install dependencies
6. Run release validation suite


Record status/conclusion for every step.


==================================================
MISSION 19 — AUDIT CREDENTIAL RUNTIME VERIFICATION
==================================================

This is the critical Step 41 proof.


For:

scripts/audit-required-checks.mjs


Verify that it successfully accesses the live branch-protection required-status
checks endpoint using the repository secret credential.


The audit must prove the live required context includes:

Run release validation suite


and that the workflow definitions can produce that context.


Record only sanitized output.


NEVER record:

Authorization header
token
secret value
partial token
token prefix/suffix


==================================================
MISSION 20 — HANDLE AUTH FAILURE SAFELY
==================================================

If the audit returns:

401
403
404 caused by authorization
resource-not-accessible
insufficient permissions
credential rejected
credential expired
credential scope mismatch


DO NOT bypass it.

DO NOT modify branch protection.

DO NOT broaden permissions automatically.

DO NOT create another credential.


Stop.


Final verdict:

RELEASE WORKFLOW EXECUTES — AUDIT CREDENTIAL REJECTED


Record:

HTTP status
sanitized GitHub error
failing step
run ID
head SHA


==================================================
MISSION 21 — HANDLE AUDIT LOGIC FAILURE SAFELY
==================================================

If authentication succeeds but the audit reports that:

Run release validation suite

is missing or mismatched in live branch protection:


DO NOT rename anything automatically.

DO NOT modify branch protection.

DO NOT weaken the audit.


Stop.


Final verdict:

RELEASE WORKFLOW EXECUTES — REQUIRED CHECK CONFIGURATION MISMATCH


Record the sanitized live contexts and workflow check names.


==================================================
MISSION 22 — RELEASE TEST VERIFICATION
==================================================

If the audit passes, allow the workflow to continue normally.


Verify the final:

Run release validation suite

test command actually executes.


Record:

test command
test counts
passed
failed
skipped
exit status


Do not call the gate successful merely because the audit passed.


==================================================
MISSION 23 — SUCCESS DEFINITION
==================================================

Release gate is VERIFIED only if ALL are true:

1. invalid `administration` workflow key absent;
2. GitHub accepts workflow syntax;
3. automatic push run created;
4. job count > 0;
5. check-run count > 0;
6. exact check exists:
   Run release validation suite
7. check head SHA == canonical main SHA;
8. checkout executes;
9. audit executes;
10. audit authenticates successfully;
11. audit reads live required-status configuration;
12. audit confirms required context;
13. pnpm setup succeeds;
14. Node setup succeeds;
15. dependencies install successfully;
16. release tests execute;
17. release tests pass;
18. required check conclusion == success;
19. local main == origin/main;
20. ahead == 0;
21. behind == 0.


Anything less is NOT a verified release gate.


==================================================
MISSION 24 — POST-PUSH CANONICAL VERIFICATION
==================================================

After workflow completion, fetch origin again.


Record:

local main SHA
origin/main SHA

local TREE
origin TREE

ahead
behind
working-tree status


If the required check succeeds, this exact SHA/tree becomes the new canonical
release candidate.


==================================================
MISSION 25 — PRESERVE CALORA RECONCILIATION WORK
==================================================

Confirm the resulting canonical main still preserves:

1. durable capture acceptance;
2. Weekly Programs deterministic modal transition/state machine;
3. shared Planner Program pools/eligibility;
4. bounded Coach request lifecycle;
5. Coach Fact Context restriction;
6. diary imageAssetKey synchronization;
7. capture compatibility;
8. capture security;
9. recipe nextOffset;
10. recipe terminalReason;
11. Premium entitlement;
12. PKCE/auth;
13. account isolation;
14. deletion fences;
15. sync ownership;
16. current API contracts;
17. release attestation;
18. Expo Router;
19. production EAS configuration;
20. Apple Health privacy configuration;
21. TestFlight configuration.


No historical branch merge is authorized.


==================================================
MISSION 26 — DATABASE / DEPLOYMENT BOUNDARY
==================================================

Confirm:

database changes: none
schema changes: none
migrations: none
manual API deployment: none
Replit production publish: none
EAS operation: none
iOS build: none
Android build: none
TestFlight submission: none
App Store submission: none


If Railway or another provider automatically reacts to the main push, record it
as an external automatic reaction only.

Do not treat it as an authorized Step 41 deployment.


==================================================
MISSION 27 — TESTFLIGHT HARD STOP
==================================================

EVEN IF EVERYTHING PASSES:

DO NOT:

run eas build
run eas submit
trigger iOS GitHub build workflow
increment iOS build number
create IPA
submit TestFlight
submit App Store
build Android


Step 41 ends at release-gate verification.


The owner will separately authorize the native/TestFlight release.


==================================================
OUTPUT REPORT
==================================================

Create:

41_CALORA_RELEASE_GATE_CREDENTIAL_INTEGRATION_AND_VERIFICATION_REPORT.md


The report MUST contain:

1. Executive summary
2. Starting canonical SHA/tree
3. Local/origin state
4. Local-only commit classification
5. Secret existence verification by NAME ONLY
6. Confirmation secret value was never accessed/exposed
7. Original invalid permission status
8. Branch-protection verification
9. Required check identity
10. Exact credential-wiring design
11. Exact workflow diff
12. Audit-script diff or confirmation none
13. Secret-exposure audit
14. YAML/workflow validation
15. Scripts test result
16. Typecheck result
17. git diff --check result
18. Application/runtime changed-path audit
19. Remediation commit SHA/tree
20. Pre-push remote verification
21. Push result
22. New canonical SHA/tree
23. Automatic workflow run identity
24. Job count
25. Check-run count
26. Required check-run identity
27. Per-step execution results
28. Audit endpoint runtime result
29. Sanitized audit output
30. Credential authorization result
31. Release validation test result
32. Required check final conclusion
33. Post-push local/origin state
34. Preserved Calora application invariants
35. Database/migration confirmation
36. Deployment/mobile-build confirmation
37. Remaining uncertainties
38. Exact recommendation for Step 42


FINAL VERDICT MUST BE EXACTLY ONE OF:

RELEASE GATE VERIFIED — READY FOR OWNER TESTFLIGHT AUTHORIZATION

RELEASE GATE CREDENTIAL MISSING — NOT PUSHED

RELEASE GATE CREDENTIAL INTEGRATION BLOCKED — REMOTE MAIN CHANGED

RELEASE GATE CREDENTIAL INTEGRATION FAILED VALIDATION — NOT PUSHED

RELEASE WORKFLOW EXECUTES — AUDIT CREDENTIAL REJECTED

RELEASE WORKFLOW EXECUTES — REQUIRED CHECK CONFIGURATION MISMATCH

RELEASE GATE EXECUTION FAILED — INVESTIGATION REQUIRED


==================================================
STOP CONDITION
==================================================

STOP after:

credential integration,
validation,
normal push if safe,
automatic release-gate execution,
verification,
and creation of the numbered Step 41 report.


DO NOT BUILD IOS.

DO NOT BUILD ANDROID.

DO NOT TRIGGER EAS.

DO NOT SUBMIT TESTFLIGHT.

DO NOT SUBMIT APP STORE.

DO NOT DEPLOY API.

DO NOT MODIFY DATABASE.

Wait for explicit owner authorization for Step 42.