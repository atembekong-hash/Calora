STEP 58D-R2 — FINAL POST-REPUBLISH PRODUCTION IDENTITY VERIFICATION

The owner has completed exactly ONE authorized Republish after successful
Step 58D-R1 publishing-source alignment.

DO NOT Publish or Republish again.
DO NOT modify source, Git, configuration, database, mobile builds, TestFlight,
Metro, domains, Railway, Coach, or providers.

Perform POST-DEPLOYMENT VERIFICATION ONLY.

1. Query:

https://calorie-coach-pie35449.replit.app/api/version

https://mycaloraapp.com/api/version

Both MUST report:

gitCommit:
5c2edf1b28c00ba86b25494d860e9b0bc69debca

sourceTree:
8eac19f420fba0640dd96188cf0be60770b3ced5

sourceDigest:
2d326de3549745fbdfb3f9db90f1a57c39768ffe940ec4e670579a6199786a05

The releaseId may contain the new Republish build timestamp.
Record its exact value.

2. Verify both:

/api/healthz

Require both healthy.

3. Confirm:

COACH_FACT_CONTEXT_ENABLED=false
RELEASE_SENSITIVE_ACTIVATION_REQUESTED=false
default-deny remains intact.

4. Perform only safe/read-only or authentication-boundary production smoke
verification for:

recipes/nutrition
Planner/program
capture approval
sync
authentication/account fences
account deletion boundary
Coach/default-deny

Do not fabricate production data.
Do not bypass authentication.
Do not mutate user data.

5. Confirm no database migration/schema/seed/backfill/manual mutation occurred.

6. Confirm current production contains the reviewed runtime behavior for:

recipe nutrition truthfulness
finite/non-negative macro validation
Planner shared program ordering
role-aware seven-day fallback
capture approval acknowledgement
diary image provenance
imageAssetKey handling
current account-scoped sync
auth/account fences
bounded Coach/default-deny

7. Confirm unchanged:

iOS TestFlight Build 7
Android versionCode 24
Railway
Metro
Fitness absent.

Create:

58D_R2_CALORA_FINAL_POST_REPUBLISH_VERIFICATION_REPORT.md

Keep the report OUTSIDE the active publishing source and UNCOMMITTED.

Do not allow report creation to change the publishing-source HEAD/tree/status.

If BOTH production aliases exactly match the approved SHA/tree/digest and all
health/safety checks pass, final verdict:

PRODUCTION API ALIGNMENT VERIFIED — RUNTIME DRIFT CLOSED

If identity differs, final verdict:

PRODUCTION PROMOTION FAILED — LIVE IDENTITY MISMATCH

Do NOT automatically rollback or republish.

STOP after the report.